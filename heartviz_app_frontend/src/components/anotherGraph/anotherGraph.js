import React from "react";

const AnotherVisualizationComponent = ({ subspaceId }) => {
  return (
    <div>
      <h2>Another Visualization</h2>
      <p>Subspace ID: {subspaceId}</p>
      {/* Replace this with another graph or visualization */}
    </div>
  );
};

export default AnotherVisualizationComponent;
